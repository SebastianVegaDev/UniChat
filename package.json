{
    "name": "unichat",
    "version": "1.0.0",
    "private": true,
    "description": "Full-stack university learning hub with realtime course chat, resources, calendars, admin workflows, AI answers, and voice playback.",
    "scripts": {
        "install:backend": "npm --prefix backend install",
        "install:frontend": "npm --prefix frontend install",
        "install:all": "npm --prefix backend install && npm --prefix frontend install",
        "dev:backend": "npm --prefix backend run dev",
        "dev:frontend": "npm --prefix frontend run dev",
        "build:backend": "npm --prefix backend run build",
        "build:frontend": "npm --prefix frontend run build",
        "build": "npm run build:backend && npm run build:frontend",
        "typecheck:backend": "npm --prefix backend run typecheck",
        "typecheck:frontend": "npm --prefix frontend run typecheck",
        "typecheck": "npm run typecheck:backend && npm run typecheck:frontend",
        "lint:frontend": "npm --prefix frontend run lint",
        "db:schema": "npm --prefix backend run db:schema",
        "db:seed": "npm --prefix backend run db:seed",
        "screenshots:readme": "npm --prefix frontend run screenshots:readme",
        "validate": "npm run typecheck && npm run build:backend && npm run lint:frontend && npm run build:frontend",
        "docker:up": "docker compose --env-file .env.docker up --build -d",
        "docker:down": "docker compose --env-file .env.docker down",
        "docker:logs": "docker compose --env-file .env.docker logs -f",
        "docker:reset": "docker compose --env-file .env.docker down -v && docker compose --env-file .env.docker up --build -d",
        "docker:ps": "docker compose --env-file .env.docker ps"
    },
    "keywords": [
        "react",
        "node",
        "express",
        "postgresql",
        "socket.io",
        "university",
        "ai"
    ],
    "author": "",
    "license": "ISC"
}
