import { BookOpen, Newspaper, ShieldCheck, Users } from "lucide-react";

export const ADMIN_SECTIONS = ["security", "courses", "news", "users"];

export const ADMIN_SECTION_ICONS = {
    security: ShieldCheck,
    courses: BookOpen,
    news: Newspaper,
    users: Users
};